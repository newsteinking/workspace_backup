from mcpi.minecraft import Minecraft
import time
logan=Minecraft.create()

colors=[0, 1, 2, 3, 4, 5]
x,y,z=logan.player.getPos()
for color in colors:
    logan.setBlock(x+3,y+color,z,35,color)
    #y+=1
    time.sleep(0.3)